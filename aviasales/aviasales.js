import { expect } from 'chai';

describe('Book a flight ticket', () => {
    before('open home page usps', () => {
        browser.url('https://www.aviasales.com/');
    });

    it('', () => {
        expect('').eq('');
    });
});